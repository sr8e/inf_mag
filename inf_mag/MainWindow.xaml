<Window x:Class="inf_mag.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:inf_mag"
        mc:Ignorable="d"
        Title="MainWindow" Height="180" Width="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="20px"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding Path=MagRate}" x:Name="MagRate" Grid.Row="1" Grid.Column="0" Height="20" Margin="10"></TextBox>
        <Button>c</Button>
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <StatusBarItem>
                <TextBlock Text="{Binding Path=StatusText}" x:Name="StatusText"></TextBlock>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding Path=VersionText}" x:Name="VersionText"></TextBlock>
                </StatusBarItem>
        </StatusBar>
    </Grid>


</Window>
